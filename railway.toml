[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn wordlist_manager.wsgi --bind 0.0.0.0:$PORT --access-logfile - --error-logfile - --log-level info"
healthcheckPath = "/healthz"
restartPolicyType = "ON_FAILURE"
